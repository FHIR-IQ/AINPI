# ainpi-probe — FHIR endpoint liveness crawler
# Pinned to major versions so reruns are reproducible
httpx>=0.27,<1
pyarrow>=17,<21
tqdm>=4.66,<5
click>=8.1,<9

# Dev / test
pytest>=8,<9
pytest-asyncio>=0.23,<1
